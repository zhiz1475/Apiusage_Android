const {app,BrowserWindow}=require('electron');const path=require('node:path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));
app.whenReady().then(async()=>{const w=new BrowserWindow({width:1080,height:800,show:false,offscreen:true,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));for(const step of [0,50,200,500,1000]){if(step===0) await w.webContents.executeJavaScript(`localStorage.removeItem('apiusagebar-theme');document.body.classList.add('electron-window');document.documentElement.classList.add('electron-window');setTheme('day');renderPreviewDemo();document.querySelector('.mode-btn[data-mode="full"]').click();void 0`);else await sleep(step-(globalThis.last||0));globalThis.last=step;const out=await w.webContents.executeJavaScript(`(()=>{const b=[...document.querySelectorAll('.mode-btn')];return {t:performance.now(),body:document.body.className,mode:document.body.dataset.mode,b:b.map(e=>({c:e.className,bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color,trans:getComputedStyle(e).transition}))}})()`);console.log(step,JSON.stringify(out));}app.exit(0)});
