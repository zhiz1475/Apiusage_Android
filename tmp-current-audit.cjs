const {app,BrowserWindow}=require('electron');const path=require('node:path');const wait=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,frame:false,width:1080,height:800,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await wait(2000);await w.webContents.executeJavaScript(`setTheme('night',false);setMode('full');setChart('bars');void 0`);await wait(800);console.log(await w.webContents.executeJavaScript(`JSON.stringify((()=>{const q=s=>document.querySelector(s),get=s=>{const e=q(s);const c=getComputedStyle(e);return {text:e.textContent.trim().slice(0,80),display:c.display,color:c.color,bg:c.backgroundColor,border:c.borderColor,rect:e.getBoundingClientRect().toJSON()}};return {mode:document.body.dataset.mode,buttons:[...document.querySelectorAll('.mode-btn')].map(e=>({text:e.textContent,class:e.className,color:getComputedStyle(e).color,border:getComputedStyle(e).borderBottomColor})),cards:[...document.querySelectorAll('.kpi-card')].map(e=>e.getBoundingClientRect().toJSON()),panel:get('.usage-panel'),balance:get('#kpiBalanceTotal'),used:get('#kpiBalanceUsed'),widgetBalance:get('#widgetBalanceMeta'),body:getComputedStyle(document.body).backgroundColor}})())`));w.destroy();app.exit(0)});
