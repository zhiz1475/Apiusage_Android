const {app,BrowserWindow}=require('electron');const path=require('node:path');const wait=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,frame:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`renderPreviewDemo();document.body.classList.add('electron-window');document.documentElement.classList.add('electron-window');setTheme('night',false);setMode('full');setChart('bars');void 0`);for(const t of [0,50,100,250,500,1000,2000,4000]){if(t)await wait(t);console.log(t,await w.webContents.executeJavaScript(`JSON.stringify((()=>{const e=document.querySelector('.mode-btn.active'),c=getComputedStyle(e);return {body:document.body.className,mode:document.body.dataset.mode,cl:e.className,border:c.borderBottomColor,color:c.color,trans:c.transition,anim:c.animationName,app:document.querySelector('.app-shell').className,view:document.querySelector('.view-full').className}})())`))}w.destroy();app.exit(0)});
