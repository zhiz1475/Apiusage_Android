const{app,BrowserWindow}=require('electron');const path=require('node:path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await sleep(300);const o=await w.webContents.executeJavaScript(`(()=>{const es=[...document.querySelectorAll('.mode-btn')];const ss=['.mode-btn.active','body.electron-window .mode-btn.active','body.electron-window:not(.night-mode) .mode-btn.active','body.electron-window .mode-btn'];document.body.classList.add('electron-window');document.documentElement.classList.add('electron-window');setTheme('day');es[1].click();return {body:document.body.className,mode:document.body.dataset.mode,es:es.map(e=>({c:e.className,m:ss.map(s=>{try{return e.matches(s)}catch(x){return String(x)}}),bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color})),sheet:[...document.styleSheets].map(s=>s.cssRules.length)}})()`);console.log(JSON.stringify(o,null,2));app.exit(0)});
