const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`localStorage.removeItem('apiusagebar-theme-v2');renderPreviewDemo();setMode('full');setTheme('night',false);setChart('heatmap');void 0`);for(const t of [0,50,300,1000]){await sleep(t);console.log(t,await w.webContents.executeJavaScript(`JSON.stringify({body:document.body.className,btns:[...document.querySelectorAll('.chart-toggle')].map(e=>{const c=getComputedStyle(e);return {cl:e.className,bg:c.backgroundColor,color:c.color,transition:c.transition}})})`))}w.destroy();app.exit(0)});
