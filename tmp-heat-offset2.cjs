const {app,BrowserWindow}=require('electron');const path=require('path');app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await new Promise(r=>setTimeout(r,1000));console.log(await w.webContents.executeJavaScript(`JSON.stringify((()=>{renderPreviewDemo();setMode('full');setChart('heatmap');const b=document.querySelector('.heatmap-body'),wd=document.querySelector('.heatmap-weekdays'),wr=document.querySelector('.heatmap-grid-wrap'),p=b.offsetParent;return {offsets:{b:b.offsetLeft,wd:wd.offsetLeft,wr:wr.offsetLeft,p:p?.className},rects:{b:b.getBoundingClientRect().toJSON(),wd:wd.getBoundingClientRect().toJSON(),wr:wr.getBoundingClientRect().toJSON()},styles:{gap:getComputedStyle(b).gap,pad:getComputedStyle(b).paddingLeft}}})())`));w.destroy();app.exit(0)});
