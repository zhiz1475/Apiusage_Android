const {app,BrowserWindow}=require('electron');const path=require('path');app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));console.log(await w.webContents.executeJavaScript(`JSON.stringify({head:document.head.innerHTML.slice(-400),styles:[...document.querySelectorAll('style')].map(s=>({id:s.id,text:s.textContent}))})`));await w.webContents.executeJavaScript(`document.head.insertAdjacentHTML('beforeend','<style id="x">body.night-mode .chart-toggle.active { background-color: lime !important; color: red !important; }</style>');void 0`);console.log(await w.webContents.executeJavaScript(`JSON.stringify({head:document.head.innerHTML.slice(-500),styles:[...document.querySelectorAll('style')].map(s=>({id:s.id,text:s.textContent}))})`));console.log(await w.webContents.executeJavaScript(`JSON.stringify({matches:document.querySelector('.chart-toggle').matches('body .chart-toggle'),bg:getComputedStyle(document.querySelector('.chart-toggle')).backgroundColor})`));w.destroy();app.exit(0)});
