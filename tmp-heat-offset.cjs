const {app,BrowserWindow}=require('electron');const path=require('path');app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await new Promise(r=>setTimeout(r,500));console.log(await w.webContents.executeJavaScript(`JSON.stringify((()=>{renderPreviewDemo();setMode('full');setChart('heatmap');const b=document.querySelector('.heatmap-body'),wd=document.querySelector('.heatmap-weekdays'),wr=document.querySelector('.heatmap-grid-wrap'),g=document.querySelector('.heatmap-grid');return {b:b.getBoundingClientRect().toJSON(),wd:wd.getBoundingClientRect().toJSON(),wr:wr.getBoundingClientRect().toJSON(),offset:{left:wr.offsetLeft,top:wr.offsetTop,parent:wr.offsetParent?.className},scroll:[b.scrollLeft,b.clientWidth,b.scrollWidth],grid:g.getBoundingClientRect().toJSON()}})())`));w.destroy();app.exit(0)});
