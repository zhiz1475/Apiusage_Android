const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,frame:false,width:1080,height:800,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true,backgroundThrottling:false}});await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`renderPreviewDemo();setMode('full');setTheme('night',false);setChart('heatmap');void 0`);await sleep(900);console.log(await w.webContents.executeJavaScript(`JSON.stringify((()=>{const q=s=>document.querySelector(s), props=s=>{const e=q(s); if(!e)return null;const c=getComputedStyle(e);return {display:c.display,color:c.color,bg:c.backgroundColor,border:c.borderColor,rect:e.getBoundingClientRect().toJSON(),text:e.textContent.slice(0,80)}};return {body:props('body'),panel:props('.panel'),card:props('.kpi-card'),tooltip:props('.data-tooltip'),toast:props('.toast'),refresh:props('.refresh-btn'),activeToggle:props('.chart-toggle.active'),activeMode:props('.mode-btn.active'),heat:props('.heat-cell.level-4'),summary:props('.heatmap-selected')}})())`));w.destroy();app.exit(0)});
