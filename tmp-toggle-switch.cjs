const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await sleep(1000);await w.webContents.executeJavaScript(`setTheme('night',false);setChart('heatmap');void 0`);for(const t of [0,50,100,200,500,1000,2000]){if(t)await sleep(t);console.log(t,await w.webContents.executeJavaScript(`JSON.stringify({active:[...document.querySelectorAll('.chart-toggle')].map(e=>({cl:e.className,text:e.textContent,bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color})),views:[...document.querySelectorAll('[data-chart-view]')].map(e=>({cl:e.className,display:getComputedStyle(e).display}))})`))}w.destroy();app.exit(0)});
