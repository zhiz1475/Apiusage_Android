const{app,BrowserWindow}=require('electron');const path=require('node:path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await sleep(500);const o=await w.webContents.executeJavaScript(`(async()=>{const b=[...document.querySelectorAll('.mode-btn')];document.body.className='electron-window';setTheme('day');const snap=async l=>{void document.body.offsetHeight;await new Promise(r=>requestAnimationFrame(r));return {l,body:document.body.className,html:document.documentElement.className,hasNight:document.documentElement.matches(':has(body.night-mode)'),b:b.map(e=>({c:e.className,bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color}))}};const a=[await snap('start')];b[0].classList.remove('active');b[1].classList.add('active');a.push(await snap('manual'));b[0].className='mode-btn active';b[1].className='mode-btn';a.push(await snap('reverse'));return a;})()`);console.log(JSON.stringify(o,null,2));app.exit(0)});
