const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`renderPreviewDemo();setTheme('night',false);setMode('full');setChart('heatmap');void 0`);await sleep(600);console.log(await w.webContents.executeJavaScript(`JSON.stringify((()=>{const e=document.querySelector('.chart-toggle.active'), b=document.body, h=document.documentElement;const c=getComputedStyle(e),cb=getComputedStyle(b),ch=getComputedStyle(h);return {matches:e.matches('body.night-mode .chart-toggle.active'),ink:[c.getPropertyValue('--ink'),cb.getPropertyValue('--ink'),ch.getPropertyValue('--ink')],bg:[c.background,c.backgroundColor],styleSheets:[...document.styleSheets].map(s=>({href:s.href,disabled:s.disabled,media:s.media?.mediaText,rules:s.cssRules?.length}))}})())`));w.destroy();app.exit(0)});
