const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`renderPreviewDemo();setMode('full');setChart('heatmap');void 0`);await sleep(100);console.log(await w.webContents.executeJavaScript(`JSON.stringify([...document.querySelectorAll('.chart-toggle')].map(e=>{const c=getComputedStyle(e);return {class:e.className,text:e.textContent,bg:c.backgroundColor,color:c.color,border:c.borderColor,css:[...document.styleSheets].flatMap(s=>{try{return [...s.cssRules].filter(r=>r.selectorText&&r.selectorText.includes('chart-toggle')).map(r=>r.cssText)}catch{return[]}})}}))`));w.destroy();app.exit(0)});
