const {app,BrowserWindow}=require('electron');const path=require('node:path');const wait=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,frame:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true,backgroundThrottling:false}});await w.loadFile(path.join(process.cwd(),'index.html'));for(const t of [0,100,300,600,1000,1500,2500]){if(t)await wait(t);console.log(t,await w.webContents.executeJavaScript(`JSON.stringify({mode:document.body.dataset.mode,body:document.body.className,btn:[...document.querySelectorAll('.mode-btn')].map(e=>e.className),views:[...document.querySelectorAll('.view')].filter(e=>e.classList.contains('active')).map(e=>e.dataset.view),cells:document.querySelectorAll('.heat-cell').length})`))}w.destroy();app.exit(0)});
