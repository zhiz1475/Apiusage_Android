const{app,BrowserWindow}=require('electron');const path=require('node:path');const fs=require('node:fs');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({width:400,height:600,show:false,offscreen:true,frame:false,transparent:false,thickFrame:true,resizable:true,minWidth:400,minHeight:600,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true,backgroundThrottling:false}});await w.loadFile(path.join(process.cwd(),'index.html'));await sleep(500);await w.webContents.executeJavaScript(`renderPreviewDemo();document.body.classList.add('electron-window');document.documentElement.classList.add('electron-window');setTheme('day');setMode('widget');void 0`);await sleep(800);await w.webContents.executeJavaScript(`requestScrollIndicatorSync();void 0`);await sleep(100);const o=await w.webContents.executeJavaScript(`(()=>{const q=s=>document.querySelector(s),keys=['.app-shell','.topbar','.view-widget','.widget-stage','.widget-card','.widget-card .widget-footer','.widget-hint','#scrollIndicator','#scrollThumb'];const out={inner:[innerWidth,innerHeight],body:[document.body.scrollWidth,document.body.scrollHeight],mode:document.body.dataset.mode,measure:typeof measureMode==='function'?measureMode('widget'):null,stageScroll:q('.widget-stage').scrollHeight,stageClient:q('.widget-stage').clientHeight,indicator:{class:q('#scrollIndicator').className,hidden:q('#scrollIndicator').getAttribute('aria-hidden')}};for(const k of keys){const e=q(k);out[k]=e?{rect:e.getBoundingClientRect().toJSON(),display:getComputedStyle(e).display,visibility:getComputedStyle(e).visibility,overflow:getComputedStyle(e).overflow}:null}return out})()`);fs.mkdirSync(path.join(process.cwd(),'dist','audit'),{recursive:true});fs.writeFileSync(path.join(process.cwd(),'dist','audit','widget-min.png'),(await w.webContents.capturePage()).toPNG());console.log(JSON.stringify(o,null,2));app.exit(0)}).catch(e=>{console.error(e);app.exit(1)});
