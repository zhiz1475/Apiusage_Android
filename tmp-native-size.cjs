const{app,BrowserWindow}=require('electron');const path=require('node:path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({width:460,height:690,show:false,offscreen:true,frame:false,transparent:false,thickFrame:true,roundedCorners:true,hasShadow:true,resizable:true,minWidth:400,minHeight:600,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await sleep(300);for(const size of [[400,500],[350,550],[640,860],[300,300]]){w.setSize(...size);await sleep(100);console.log(JSON.stringify({requested:size,bounds:w.getBounds(),content:[await w.webContents.executeJavaScript('innerWidth'),await w.webContents.executeJavaScript('innerHeight')]}));}app.exit(0)});
