const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));for(const t of [0,100,500,1000,2000]){if(t)await sleep(t);console.log(t,await w.webContents.executeJavaScript(`JSON.stringify({body:document.body.className,active:[...document.querySelectorAll('.chart-toggle')].map(e=>({cl:e.className,text:e.textContent,bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color})),n:document.querySelectorAll('.heat-cell').length})`))}w.destroy();app.exit(0)});
