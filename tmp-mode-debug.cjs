const{app,BrowserWindow}=require('electron');const path=require('node:path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await sleep(500);const o=await w.webContents.executeJavaScript(`(()=>{const b=[...document.querySelectorAll('.mode-btn')];const snap=l=>({label:l,body:document.body.className,mode:document.body.dataset.mode,b:b.map(e=>({text:e.textContent,cls:e.className,outer:e.outerHTML,focus:document.activeElement===e,bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color}))});const a=[snap('before')];document.body.classList.add('electron-window');document.documentElement.classList.add('electron-window');setTheme('day');a.push(snap('after-theme'));setMode('full');a.push(snap('after-setMode'));return a;})()`);console.log(JSON.stringify(o,null,2));app.exit(0)});
