const {app,BrowserWindow}=require('electron'); const path=require('path'); app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}}); await w.loadFile(path.join(process.cwd(),'index.html')); await w.webContents.executeJavaScript(`renderPreviewDemo(); setMode('full'); void 0`); await new Promise(r=>setTimeout(r,500)); console.log(await w.webContents.executeJavaScript(`JSON.stringify([...document.querySelectorAll('.mode-btn')].map(x=>({text:x.textContent,class:x.className,sel:x.getAttribute('aria-selected'),rect:x.getBoundingClientRect().toJSON(),color:getComputedStyle(x).color,border:getComputedStyle(x).borderBottomColor})))`)); console.log(await w.webContents.executeJavaScript(`JSON.stringify({mode:document.body.dataset.mode,view:[...document.querySelectorAll('.view')].map(x=>({c:x.className,display:getComputedStyle(x).display}))})`)); app.exit(0)});
