const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`renderPreviewDemo();setTheme('night',false);setMode('full');setChart('heatmap');document.head.insertAdjacentHTML('beforeend','<style id="x">body.night-mode .chart-toggle.active { background-color: lime !important; color: red !important; }</style>');void 0`);for(const t of [0,10,100,500,1500]){await sleep(t);console.log(t,await w.webContents.executeJavaScript(`JSON.stringify({style:!!document.querySelector('#x'),body:document.body.className,active:[...document.querySelectorAll('.chart-toggle')].map(e=>({cl:e.className,text:e.textContent,bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color}))})`))}w.destroy();app.exit(0)});
