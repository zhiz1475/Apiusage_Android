const {app,BrowserWindow}=require('electron');const path=require('path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`localStorage.removeItem('apiusagebar-theme-v2');renderPreviewDemo();setMode('full');setTheme('night',false);setChart('heatmap');void 0`);await sleep(1000);for(const js of [`(()=>{const e=document.querySelector('.chart-toggle.active');return JSON.stringify({class:e.className,anc:e.closest('body').className,bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color,offset:e.offsetWidth})})()`,`(()=>{const e=document.querySelector('.chart-toggle.active');e.style.background='red';e.style.color='blue';return JSON.stringify({bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color})})()`,`(()=>{const e=document.querySelector('.chart-toggle.active');e.style.removeProperty('background');e.style.removeProperty('color');e.classList.remove('active');e.classList.add('active');return JSON.stringify({bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color})})()`]) console.log(await w.webContents.executeJavaScript(js));w.destroy();app.exit(0)});
