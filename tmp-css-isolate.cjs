const{app,BrowserWindow}=require('electron');const path=require('node:path');const sleep=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});await w.loadFile(path.join(process.cwd(),'index.html'));await sleep(400);const o=await w.webContents.executeJavaScript(`(()=>{document.body.className='electron-window'; const d=document.createElement('div');d.innerHTML='<button class="mode-btn">A</button><button class="mode-btn active">B</button><button class="mode-btn active">C</button>';document.body.append(d); return [...d.querySelectorAll('button')].map(e=>({cls:e.className,m:e.matches('.mode-btn.active'),bg:getComputedStyle(e).backgroundColor,color:getComputedStyle(e).color}));})()`);console.log(JSON.stringify(o,null,2));app.exit(0)});
