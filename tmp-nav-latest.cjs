const {app,BrowserWindow}=require('electron');const path=require('node:path');const wait=ms=>new Promise(r=>setTimeout(r,ms));app.whenReady().then(async()=>{const w=new BrowserWindow({show:false,offscreen:true,frame:false,width:1080,height:800,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true,backgroundThrottling:false}});for(const [name,theme] of [['day','day'],['night','night'],['day2','day']]){await w.loadFile(path.join(process.cwd(),'index.html'));await w.webContents.executeJavaScript(`localStorage.removeItem('apiusagebar-theme');if(typeof renderPreviewDemo==='function')renderPreviewDemo();document.body.classList.add('electron-window');document.documentElement.classList.add('electron-window');if(typeof setTheme==='function')setTheme('${theme}',false);if(typeof setMode==='function')setMode('full');if(typeof setChart==='function')setChart('bars');void 0`);await wait(900);await w.webContents.executeJavaScript(`(async()=>{for(let i=0;i<30;i++){const v=document.querySelector('.view.active');if(v&&getComputedStyle(v).display!=='none'&&v.getBoundingClientRect().height>0&&v.innerText.trim()){await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));return true}await new Promise(r=>setTimeout(r,50))}return false})()`);await wait(180);console.log(name,await w.webContents.executeJavaScript(`JSON.stringify({body:document.body.className,mode:document.body.dataset.mode,buttons:[...document.querySelectorAll('.mode-btn')].map(e=>({text:e.textContent,class:e.className,aria:e.getAttribute('aria-selected'),rect:e.getBoundingClientRect().toJSON(),border:getComputedStyle(e).borderBottomColor,color:getComputedStyle(e).color})),activeView:[...document.querySelectorAll('.view')].filter(e=>e.classList.contains('active')).map(e=>e.dataset.view)})`));}w.destroy();app.exit(0)});
